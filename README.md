# Setup
This contains the initial C++23 environment setup.
My OS is Omarchy, which 

## How to Compile & Run

`clang++` (version 22.1.5 or newer with `-std=c++23` support)

### 1. Compile
```bash
cd Exercise_
clang++ -std=c++23 ToolChain.cpp -o ToolChain
```

### 2. Run
```bash
./ToolChain
```
